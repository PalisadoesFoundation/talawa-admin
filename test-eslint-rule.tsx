import React from 'react';
import { DataGrid } from '@mui/x-data-grid';

// This is a test file to demonstrate the ESLint rule
export const TestComponent = (): JSX.Element => {
  return <div>Test</div>;
};
