[**talawa-admin**](README.md)

***

# Function: handleTokenRefresh()

> **handleTokenRefresh**(): `Promise`\<`void`\>

Defined in: [src/utils/getRefreshToken.ts:54](https://github.com/SujalTripathi/talawa-admin/blob/201c311285eee8900b55c8a032a23046ba8c861b/src/utils/getRefreshToken.ts#L54)

Attempts to refresh the token and reload the page if successful.
Falls back to clearing storage and redirecting to login if refresh fails.

## Returns

`Promise`\<`void`\>
