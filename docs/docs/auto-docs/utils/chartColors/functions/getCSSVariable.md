[Admin Docs](/)

***

# Function: getCSSVariable()

> **getCSSVariable**(`variableName`): `string`

Defined in: [src/utils/chartColors.ts:12](https://github.com/PalisadoesFoundation/talawa-admin/blob/main/src/utils/chartColors.ts#L12)

Helper function to get CSS variable values from the document root.

## Parameters

### variableName

`string`

The name of the CSS variable (e.g., '--chart-green')

## Returns

`string`

The value of the CSS variable, or an empty string if not available
